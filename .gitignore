# Runtime dependencies are installed at image build time, never committed.
vendor/
node_modules/

# Test and tooling caches
.phpunit.cache/
.phpunit.result.cache
*.log

# Local overrides
.env.local
docker-compose.override.yml
